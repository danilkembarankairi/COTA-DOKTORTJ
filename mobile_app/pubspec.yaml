name: smart_irrigation
description: Smart Irrigation System with IoT
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  mqtt_client: ^10.0.0
  http: ^1.1.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  fl_chart: ^0.65.0
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/logo_cota.png
